# Operator-only identifiers. Copy to .env.ops (gitignored). Do not commit real values.
# These are not app runtime secrets. They are names we do not want in the public tree.
# Agents: see AGENTS.md ("Do not") and .cursor/rules/ops-identifiers.mdc.

# Neon console project name for botpasses-prod (restore drills).
NEON_PROD_PROJECT=

# Linear issue URL for BOTP-10 (workspace slug is private).
LINEAR_BOTP10_URL=
