app = "botpasses-staging"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

# DDL runs once per release, before the new Machine starts, over DATABASE_URL_DIRECT
# (falls back to DATABASE_URL). Boot itself no longer runs schema changes.
[deploy]
  release_command = "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/migrate.ts"

# One Machine per app. In-process TOTP enrollment state and per-IP limiters are not shared;
# do not `fly scale count` above 1 until they are store-backed.
kill_timeout = "30s"

[env]
  VAULT_MODE = "hosted"
  VAULT_DEPLOY_PLANE = "staging"
  VAULT_PUBLIC_URL = "https://staging.botpasses.com"
  PORT = "8788"

[http_service]
  internal_port = 8788
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [http_service.http_options]
    idle_timeout = 600

  [[http_service.checks]]
    interval = "15s"
    timeout = "5s"
    grace_period = "60s"
    method = "GET"
    path = "/ready"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
  cpus = 1
