# Task: security-hardening

## Outcome

Hosted and local Botpasses match the grant-vault trust model: KMS-wrapped KEK on staging/prod after cutover (expand/contract so the first image still boots on raw `VAULT_KEK`), no human reveal path, OWASP/MCP surface hardening, local AAD + loopback auth, CI audit. `npm test && npm run typecheck` green. Public docs do not claim zero-knowledge. No invented MCP session store.

## Spec pointer

- Plan: [docs/plans/2026-08-31-security-hardening.md](../../../docs/plans/2026-08-31-security-hardening.md)
- Cursor plan: Security hardening (CreatePlan)

## Topology

- Choice: pipeline
- Escalation test 1 (disjoint files / no data dep): FAIL — T-03/T-04 share `src/hosted/kernel.ts`, `src/store/types.ts`, `src/store/postgres.ts`, `src/store/sqlite-hosted.ts`; T-03/T-05 share `src/cli.ts`. Not a graph.
- Escalation test 2 (independent verifiers): PASS if sequenced — each unit has a focused `node --test` file list — but test 1 failed, so pipeline.
- Rationale: One threat model must stay coherent; overlapping hosted and store files make parallel units unsafe on the shared trunk.

## Shared contract

- Path: N/A — pipeline stages consume the plan ADRs and `KekProvider` from T-02 after that stage merges.
- Editor: `decompose` only if a split is required; implement-node units do not edit this file's allowlists.

## Full-suite verifier

`npm test && npm run typecheck`

## Units

### U-01: Trust docs

- Goal: ADR 0003, ADR 0004, threat-model doc, README honesty
- File allowlist: `docs/adr/0003-grant-vault-trust-model.md`, `docs/adr/0004-kms-wrapped-kek.md`, `docs/security/threat-model.md`, `README.md`, `.loadout/tasks/security-hardening/TASK.md`
- Exposes / consumes: D-01, D-02, D-10 language
- Verifier: `test ! "$(rg -i 'zero-knowledge' README.md src/ || true)"` and files exist
- Done-condition: ADRs accepted; README threat table names KMS + inject + REQUIRE_KMS
- Status: pending

### U-02: Crypto primitives

- Goal: AAD tests, `zeroKey`, `KekProvider`, FakeKms, `@aws-sdk/client-kms`
- File allowlist: `src/crypto.ts`, `src/hosted/kek.ts`, `src/hosted/kms.ts`, `test/crypto.test.ts`, `test/kek.test.ts`, `package.json`, `package-lock.json`
- Exposes / consumes: `KekProvider`, `KmsKekProvider`, `LocalKekProvider`, `FakeKms`, `zeroKey`
- Verifier: `node --experimental-strip-types --disable-warning=ExperimentalWarning --test --test-reporter=spec test/crypto.test.ts test/kek.test.ts`
- Done-condition: AC-04 and AAD primitive tests pass
- Status: pending

### U-03: Hosted KMS boot and rotation

- Goal: Expand/contract plane boot, unwrap at start, `rotateKek(old, new)`, kek-wrap/kek-rotate CLI, runbook
- File allowlist: `src/hosted/boot.ts`, `src/hosted/main.ts`, `src/hosted/kernel.ts`, `src/store/types.ts`, `src/store/postgres.ts`, `src/store/sqlite-hosted.ts`, `src/cli.ts`, `docs/ops/kek-rotation.md`, `test/hosted.test.ts`, `test/kek-rotate.test.ts`
- Exposes / consumes: `KekProvider.unwrap` from U-02; `listOrgs`, `updateOrgWrappedDek`
- Verifier: `node --experimental-strip-types --disable-warning=ExperimentalWarning --test --test-reporter=spec test/hosted.test.ts test/kek-rotate.test.ts`
- Done-condition: AC-01, AC-02, AC-02b, AC-02c, AC-03, AC-05
- Status: pending

### U-04: Hosted surfaces

- Goal: Headers, CORS 403, collect auth + GET need, store-backed rate limit, token rotate. No session map.
- File allowlist: `src/hosted/http.ts`, `src/hosted/collect-page.ts`, `src/hosted/operator-page.ts`, `src/hosted/rate-limit.ts`, `src/hosted/kernel.ts`, `src/hosted/auth.ts`, `src/store/schema.ts`, `src/store/types.ts`, `src/store/postgres.ts`, `src/store/sqlite-hosted.ts`, `test/hosted-operator-page.test.ts`, `test/hosted-mcp-find.test.ts`, `test/rate-limit.test.ts`, `test/isolation.test.ts`, `test/need-items.test.ts`, `test/security-headers.test.ts`
- Exposes / consumes: existing kernel + GET need + rotate + store limiter
- Verifier: `node --experimental-strip-types --disable-warning=ExperimentalWarning --test --test-reporter=spec test/hosted.test.ts test/hosted-operator-page.test.ts test/hosted-mcp-find.test.ts test/rate-limit.test.ts test/isolation.test.ts test/need-items.test.ts test/security-headers.test.ts`
- Done-condition: AC-09–AC-17
- Status: pending

### U-05: Local vault hardening

- Goal: AAD migrate-on-open, loopback HMAC bearer
- File allowlist: `src/vault.ts`, `src/db.ts`, `src/server.ts`, `src/operator-page.ts`, `src/cli.ts`, `test/vault.test.ts`, `test/http.test.ts`, `test/mcp.test.ts`, `test/cli.test.ts`
- Exposes / consumes: `encrypt` AAD from U-02
- Verifier: `node --experimental-strip-types --disable-warning=ExperimentalWarning --test --test-reporter=spec test/vault.test.ts test/http.test.ts test/mcp.test.ts test/cli.test.ts`
- Done-condition: AC-06, AC-07, AC-08
- Status: pending

### U-06: CI and doc close

- Goal: npm audit --omit=dev, Dependabot, changelog, cutover secrets list, AGENTS.md if needed
- File allowlist: `.github/workflows/ci.yml`, `.github/dependabot.yml`, `README.md`, `CHANGELOG.md`, `docs/ops/botpasses-cutover.md`, `docs/ops/kek-rotation.md`, `AGENTS.md`
- Exposes / consumes: all prior behavior
- Verifier: `npm test && npm run typecheck`
- Done-condition: AC-16, AC-18, DoD
- Status: pending

## Merge order

1. U-01
2. U-02
3. U-03
4. U-04
5. U-05
6. U-06

U-04 and U-05 are sequential even though hosted vs local files are mostly disjoint, because `src/cli.ts` appears in U-03 and U-05 and U-04 isolation tests assume boot rules from U-03.

## Isolation mode

- shared-trunk
- Reason: `shared-working-tree` is installed; user did not ask for worktrees.

## Notes / failures

- Do not claim zero-knowledge in any unit.
- Do not invent an MCP session store.
- Do not add `migrations/003_rate_limit.sql`.
- Do not hand-merge `package-lock.json`.
- Do not stage/commit unless the user asks.
